# Copy to `.env` next to docker-compose.prod.yml on the VPS, then edit.

# Your domain, with no https:// and no www.
DOMAIN=example.com

# Public address of the API. Keep it on the same domain as the storefront so
# login cookies are first-party. Must match DOMAIN above.
VITE_API_URL=https://example.com/api
