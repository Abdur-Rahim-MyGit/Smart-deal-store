# Storefront environment (copy to .env for local overrides)

# Base URL of the Smart Deal API, including /api.
# Defaults to http://localhost:5050/api when unset.
VITE_API_URL=http://localhost:5050/api
